import cv2
import numpy as np

def RESULTS(Mean, Std, total_points_used, imagePoints):
    # return WorldPoints

    WorldPoints = np.zeros((total_points_used, 3), dtype=np.float32)


    return WorldPoints


print(RESULTS(0,0,9,0))